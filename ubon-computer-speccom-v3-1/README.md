# Ubon Computer SpecCom V.1 (Plus) — Vercel Ready
- เพิ่มระบบ Option เสริม: Monitor, Software (เลือกหลายรายการ + กำหนดจำนวน)
- UI ทันสมัยขึ้น (gradient header, sticky total)
- Excel/CSV import รองรับหมวดใหม่

Deploy:
- `npm run build`
- Output: `dist`
